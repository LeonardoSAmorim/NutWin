// NutWin - Programa de Apoio a Nutrição(R)
// Copyright (C) 2002-2010 Departamento de Informática em Saúde
// Universidade Federal de São Paulo - UNIFESP <www.unifesp.br>
//
// This file is part of NutWin.
//
// NutWin is free software:  you  can  redistribute  it  and/or
// modify it under the terms of the GNU General Public  License
// as published by the Free Software Foundation, either version
// 3 of the License, or (at your option) any later version.
//
// Nutwin is distributed in the hope that it  will  be  useful,
// but WITHOUT ANY WARRANTY; without even the implied  warranty
// of MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See
// the GNU General Public License for more details.
//
// You should have received a copy of the  GNU  General  Public
// License along with Foobar.
// If not, see <http://www.gnu.org/licenses/>.




{ ****************************************************************** }
{                                                                    }
{   VCL component TEquivalenteProteina                               }
{                                                                    }
{   Equivalente de Proteina                                          }
{                                                                    }
{   Code generated by Component Create for Delphi                    }
{                                                                    }
{   Generated from source file                                       }
{   on 04 Oct 1999 at 08:30                                          }
{                                                                    }
{   Copyright © 1999 by CIS/EPM                                      }
{                                                                    }
{ ****************************************************************** }

unit EquProteina;

interface

uses
  Windows, Messages, SysUtils, Classes, Graphics, Controls, Forms, Dialogs,
  db, dbtables, CAlimento, NutCnst, CCSListaLinks, DelayedOpIndicator;

type
  TEquivalenteProteina = class(TCCSListaLinks)
  private
    FListaDeGruposEquivalentes: TDataSource;
    FListaDeAlimentosEquivalentes: TDataSource;
    FFiltrarDoInicio: Boolean;
    FPeso: Double;
    FFiltrarNomeAlimento: String;
    FLocalizaNomeAlimento: String;
    FAlimento: TAlimento;
    FOrdenarEquivalentePor: TOrdenarEquivalente;
    FTipoDeOrdem: TTipoOrdem;
    FAtivar: Boolean;
    FDelayedOpIndicator: TDelayedOpIndicator;
        { Method to set variable and property values and create objects }
        procedure AutoInitialize;
        { Method to free any objects created by AutoInitialize }
        procedure AutoDestroy;
    procedure SetListaDeAlimentosEquivalentes(const Value: TDataSource);
    procedure SetListaDeGruposEquivalentes(const Value: TDataSource);
    procedure SetAlimento(const Value: TAlimento);
    procedure SetFiltrarDoInicio(const Value: Boolean);
    procedure SetFiltrarNomeAlimento(const Value: String);
    procedure SetLocalizaNomeAlimento(const Value: String);
    procedure SetOrdenarEquivalentePor(const Value: TOrdenarEquivalente);
    procedure SetPeso(const Value: Double);
    procedure SetTipoDeOrdem(const Value: TTipoOrdem);
    procedure SetAtivar(const Value: Boolean);
    procedure SetDelayedOpIndicator(const Value: TDelayedOpIndicator);
    { Private declarations }
  protected
    { Protected declarations }
      procedure Loaded; override; 
      procedure Notification(AComponent : TComponent; Operation : TOperation); override;

      function SQLSelectBasico : String;
      function SQLOrdemNome : String;
      function SQLOrdemMedida : String;
      procedure ExecSQL;
  public
    { Public declarations }
        constructor Create(AOwner: TComponent); override;
        destructor Destroy; override;
        procedure Refresh; override;
        { Localiza Medida }
        function Localizar( Nome : String; Options: TLocateOptions ) : Boolean;
   published
      { Published properties  }
        { Indicador de Operação Demorada }
        property DelayedOpIndicator : TDelayedOpIndicator read FDelayedOpIndicator write SetDelayedOpIndicator;
        { Palavra a ser pesquisada }
        property FiltrarNomeAlimento : String read FFiltrarNomeAlimento write SetFiltrarNomeAlimento;
        { Indica se a pesquisa da palavra eh do inicio ou nao }
        property FiltrarDoInicio : Boolean read FFiltrarDoInicio write SetFiltrarDoInicio;
        { Campos a serem ordenados }
        property OrdenarEquivalentePor : TOrdenarEquivalente read FOrdenarEquivalentePor write SetOrdenarEquivalentePor;
        { Sentido da ordenacao }
        property TipoDeOrdem : TTipoOrdem read FTipoDeOrdem write SetTipoDeOrdem;
        property ListaDeGruposEquivalentes : TDataSource read FListaDeGruposEquivalentes write SetListaDeGruposEquivalentes;
        { Lista de Alimentos }
        property ListaDeAlimentosEquivalentes : TDataSource read FListaDeAlimentosEquivalentes write SetListaDeAlimentosEquivalentes;
        { Texto a Localizar }
        property LocalizaNomeAlimento : String read FLocalizaNomeAlimento write SetLocalizaNomeAlimento;
        property Peso : Double read FPeso write SetPeso;
        property Alimento : TAlimento read FAlimento write SetAlimento;
        property Ativar : Boolean read FAtivar write SetAtivar;

  end;

procedure Register;

implementation

procedure Register;
begin
  RegisterComponents('Nutricao', [TEquivalenteProteina]);
end;

{ TEquivalenteProteina }

procedure TEquivalenteProteina.AutoDestroy;
begin

end;

procedure TEquivalenteProteina.AutoInitialize;
begin

end;

constructor TEquivalenteProteina.Create(AOwner: TComponent);
begin
     { Call the Create method of the parent class }
     inherited Create(AOwner);

     { AutoInitialize sets the initial values of variables and      }
     { properties; also, it creates objects for properties of       }
     { standard Delphi object types (e.g., TFont, TTimer,           }
     { TPicture) and for any variables marked as objects.           }
     { AutoInitialize method is generated by Component Create.      }
     AutoInitialize;

     { Code to perform other tasks when the component is created }

end;

destructor TEquivalenteProteina.Destroy;
begin
     { AutoDestroy, which is generated by Component Create, frees any   }
     { objects created by AutoInitialize.                               }
     AutoDestroy;

     { Here, free any other dynamic objects that the component methods  }
     { created but have not yet freed.  Also perform any other clean-up }
     { operations needed before the component is destroyed.             }

     { Last, free the component by calling the Destroy method of the    }
     { parent class.                                                    }
     inherited Destroy;
end;

procedure TEquivalenteProteina.ExecSQL;
begin
   if not Assigned( FAlimento ) or not FAtivar then
      exit;
   with TQuery( FAlimento.DMUmAlimento.dsAliEquProteina.Dataset ) do
   begin
      Close;
{      SQL.Clear;
      if OrdenarEquivalentePor = oeAlimento then
         SQL.Add( SQLOrdemNome )
      else if OrdenarEquivalentePor = oeMedida then
         SQL.Add( SQLOrdemMedida );
}
      // indica operação demorada;
      if Assigned( FDelayedOpIndicator ) then
         FDelayedOpIndicator.Start;
      try
         Open;
      finally
         // finaliza indicação de operação demorada
         if Assigned( FDelayedOpIndicator ) then
            FDelayedOpIndicator.Finish;
      end;
   end;
end;

procedure TEquivalenteProteina.Loaded;
begin
   inherited Loaded;

   if not FAtivar then
      exit;
   { Perform any component setup that depends on the property
     values having been set }
   with FAlimento.DMUmAlimento.dsGruEquProteina.DataSet do
   begin
      Open;
   end;
   ExecSQL;
end;

function TEquivalenteProteina.Localizar(Nome: String;
  Options: TLocateOptions): Boolean;
begin
   Result := FAlimento.DMUmAlimento.quAliEquProteina.Locate( 'NOME', Nome, Options );
end;

procedure TEquivalenteProteina.Notification(AComponent: TComponent;
  Operation: TOperation);
begin
     inherited Notification(AComponent, Operation);
     if Operation <> opRemove then
        Exit;
     { Has a component referenced by a property of
       this component been deleted?  If so, update
       the property. }
     if AComponent = FListaDeGruposEquivalentes then
        FListaDeGruposEquivalentes := nil
     else if AComponent = FListaDeAlimentosEquivalentes then
        FListaDeAlimentosEquivalentes := nil
     else if AComponent = FAlimento then
        FAlimento := nil
     else if AComponent = FDelayedOpIndicator then
        FDelayedOpIndicator := nil;
end;

procedure TEquivalenteProteina.Refresh;
begin
   ExecSQL;
   inherited;
end;

procedure TEquivalenteProteina.SetAlimento(const Value: TAlimento);
begin
  FAlimento := Value;
  if Assigned( Value ) then
     Value.FreeNotification(self);
  ExecSQL;
end;

procedure TEquivalenteProteina.SetAtivar(const Value: Boolean);
begin
  if not Assigned( FAlimento ) then
     begin
        FAtivar := False;
        exit;
     end   
  else if FAtivar = Value then
  else if Value then
     begin
        FAtivar := Value;
        FAlimento.DMUmAlimento.dsGruEquProteina.DataSet.Open;
        ExecSQL;
     end
  else
     begin
        FAtivar := Value;
        TQuery( FAlimento.DMUmAlimento.dsAliEquProteina.Dataset ).Close;
        FAlimento.DMUmAlimento.dsGruEquProteina.DataSet.Close;
     end;
end;

procedure TEquivalenteProteina.SetDelayedOpIndicator(
  const Value: TDelayedOpIndicator);
begin
  FDelayedOpIndicator := Value;
  if Assigned( Value ) then
     Value.FreeNotification(self);
end;

procedure TEquivalenteProteina.SetFiltrarDoInicio(const Value: Boolean);
begin
  FFiltrarDoInicio := Value;
  ExecSQL;
end;

procedure TEquivalenteProteina.SetFiltrarNomeAlimento(const Value: String);
begin
  FFiltrarNomeAlimento := Value;
  ExecSQL;
end;

procedure TEquivalenteProteina.SetListaDeAlimentosEquivalentes(
  const Value: TDataSource);
begin
  FListaDeAlimentosEquivalentes := Value;
  if Assigned (Value) then
  begin
     Value.FreeNotification(self);
     if Assigned( Alimento ) then
        Value.DataSet := Alimento.DMUmAlimento.dsAliEquProteina.DataSet;
  end;      
end;

procedure TEquivalenteProteina.SetListaDeGruposEquivalentes(
  const Value: TDataSource);
begin
  FListaDeGruposEquivalentes := Value;
  if Assigned (Value) then
  begin
     Value.FreeNotification(self);
     if Assigned( Alimento ) then
        Value.DataSet := FAlimento.DMUmAlimento.dsGruEquProteina.DataSet;
  end;      
end;

procedure TEquivalenteProteina.SetLocalizaNomeAlimento(const Value: String);
begin
  FLocalizaNomeAlimento := Value;
  Localizar( FLocalizaNomeAlimento, [loCaseInsensitive,loPartialKey] );
end;

procedure TEquivalenteProteina.SetOrdenarEquivalentePor(
  const Value: TOrdenarEquivalente);
begin
  FOrdenarEquivalentePor := Value;
  ExecSQL;
end;

procedure TEquivalenteProteina.SetPeso(const Value: Double);
begin
  FPeso := Value;
{  if FPeso <= 0 then
     FPeso := 100;}
  ExecSQL;
end;

procedure TEquivalenteProteina.SetTipoDeOrdem(const Value: TTipoOrdem);
begin
  FTipoDeOrdem := Value;
  ExecSQL;
end;

function TEquivalenteProteina.SQLOrdemMedida: String;
begin
 Result := SQLSelectBasico +
   ' FROM ' +
   'MEDIDAS, ' +
   'ALIGPROT, ' +
   'ALIMENTO ' +

   ' WHERE (ALIGPROT.IDMEDCAS = MEDIDAS.IDMEDCAS) AND ' +
   ' (ALIGPROT.IDGRUPROT = :IDGRUPROT ) AND ' +
   ' (ALIGPROT.IDALI = ALIMENTO.IDALI ) ';

 if FFiltrarNomeAlimento <> '' then
 begin
    if FFiltrarDoInicio then
       Result := Result + ' AND ( UPPER(NOME) LIKE UPPER(''' + FFiltrarNomeAlimento + '%'' ))'
    else
       Result := Result + ' AND ( UPPER(NOME) LIKE UPPER(''%' + FFiltrarNomeAlimento + '%'' ))'
 end;

 Result := Result + ' ORDER BY ALIMENTO.NOME';

 if FTipoDeOrdem = toDecrescente then
    Result := Result + ' DESC'
 else
    Result := Result + ' ASC';

end;

function TEquivalenteProteina.SQLOrdemNome: String;
begin

 Result := SQLSelectBasico +
   ' FROM ' +
   'MEDIDAS, ' +
   'ALIGPROT, ' +
   'ALIMENTO ' +

   ' WHERE (ALIGPROT.IDMEDCAS = MEDIDAS.IDMEDCAS) AND ' +
   ' (ALIGPROT.IDGRUPROT = :IDGRUPROT ) AND ' +
   ' (ALIGPROT.IDALI = ALIMENTO.IDALI ) ';

 if FFiltrarNomealimento <> '' then
 begin
    if FFiltrarDoInicio then
       Result := Result + ' AND ( UPPER(NOME) LIKE UPPER(''' + FFiltrarNomeAlimento + '%'' ))'
    else
       Result := Result + ' AND ( UPPER(NOME) LIKE UPPER(''%' + FFiltrarNomeAlimento + '%'' ))'
 end;

 Result := Result + ' ORDER BY ALIMENTO.NOME';

 if FTipoDeOrdem = toDecrescente then
    Result := Result + ' DESC'
 else
    Result := Result + ' ASC';


end;

function TEquivalenteProteina.SQLSelectBasico: String;
begin
{   Result := 'SELECT ALIGPROT.QTDE, ALIGPROT.MEDGR, Medidas.MEDIDA, ALIGPROT.IDGRUPROT, ' +
                    'ALIGPROT.IDALI, Alimento.NOME ' +
             'FROM Medidas, ALIGPROT, Alimento ' +
             'WHERE  (ALIGPROT.IDMEDCAS = Medidas.IDMEDCAS) AND ' +
                    '(ALIGPROT.IDGRUPROT = :IDGRUPROT ) AND ' +
                    '(ALIGPROT.IDALI = Alimento.IDALI )'
}
  Result :=
   'SELECT ' +
   'ALIGPROT.QTDE, ' +
   'ALIGPROT.MEDGR, ' +
   'MEDIDAS.MEDIDA, ' +
   'ALIGPROT.IDGRUPROT, ' +
   'ALIGPROT.IDALI, ' +
   'ALIMENTO.NOME ';

end;

end.

