// NutWin - Programa de Apoio a Nutrição(R)
// Copyright (C) 2002-2010 Departamento de Informática em Saúde
// Universidade Federal de São Paulo - UNIFESP <www.unifesp.br>
//
// This file is part of NutWin.
//
// NutWin is free software:  you  can  redistribute  it  and/or
// modify it under the terms of the GNU General Public  License
// as published by the Free Software Foundation, either version
// 3 of the License, or (at your option) any later version.
//
// Nutwin is distributed in the hope that it  will  be  useful,
// but WITHOUT ANY WARRANTY; without even the implied  warranty
// of MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See
// the GNU General Public License for more details.
//
// You should have received a copy of the  GNU  General  Public
// License along with Foobar.
// If not, see <http://www.gnu.org/licenses/>.




{ ****************************************************************** }
{                                                                    }
{   Delphi component TPessoa                                         }
{                                                                    }
{   Definicao das pessoas                                            }
{                                                                    }
{   Code generated by Component Create for Delphi                    }
{                                                                    }
{   Generated from source file c:\componentesdelphi\upessoa.cd       }
{   on 12 June 1997 at 9:12                                          }
{                                                                    }
{   Copyright © 1997 by CIS-EPM                                      }
{                                                                    }
{ ****************************************************************** }

unit UPessoa;

interface

uses WinTypes, WinProcs, Messages, SysUtils, Classes, Controls, 
     Forms, Graphics, Stdctrls, Mask;

{ Unit-wide declarations }
{ type }
     { . . . }
{ var }
     { . . . }


type
  TPessoa = class(TComponent)
    private
      { Private fields of TPessoa }
        { Storage for property PrimeiroNome }
        FPrimeiroNome : TCustomEdit;
        { Storage for property Sobrenome }
        FSobrenome : TCustomEdit;
        { Storage for property DataNascimento }
        FDataNascimento : TCustomEdit;
        { Storage for property BotaoNovo }
        FBotaoNovo : TButton;
        { Pointer to application's OnNovaPessoa handler, if any }
        FOnNovaPessoa : TNotifyEvent;

      { Private methods of TPessoa }
        { Method to set variable and property values and create objects }
        procedure AutoInitialize;
        { Method to free any objects created by AutoInitialize }
        procedure AutoDestroy;
        { Write method for property BotaoNovo }
        procedure SetBotaoNovo(Value : TButton);

    protected
      { Protected fields of TPessoa }

      { Protected methods of TPessoa }
        { Resets prop of component type if referenced component deleted }
        procedure Notification(AComponent : TComponent; Operation : TOperation); override;
        { Method to generate OnNovaPessoa event }
        procedure NovaPessoa;
        procedure Loaded; override;

    public
      { Public fields and properties of TPessoa }

      { Public methods of TPessoa }
        constructor Create(AOwner: TComponent); override;
        destructor Destroy; override;
        function Execute : Boolean;
        procedure CadastraNovaPessoa (Sender: TObject);

    published
      { Published properties of the component }
        { E criada uma pessoa }
        property OnNovaPessoa : TNotifyEvent read FOnNovaPessoa write FOnNovaPessoa;
        { Nome da Pessoa }
        property PrimeiroNome : TCustomEdit read FPrimeiroNome write FPrimeiroNome;
        property Sobrenome : TCustomEdit read FSobrenome write FSobrenome;
        property DataNascimento : TCustomEdit
             read FDataNascimento write FDataNascimento;
        property BotaoNovo : TButton read FBotaoNovo write SetBotaoNovo;

  end;

procedure Register;

implementation
uses CadPess;

procedure Register;
begin
     { Register TPessoa with Nutricao as its
       default page on the Delphi component palette }
     RegisterComponents('Nutricao', [TPessoa]);
end;

{ Method to set variable and property values and create objects }
procedure TPessoa.AutoInitialize;
begin
     FPrimeiroNome := nil;
     FSobrenome := nil;
     FDataNascimento := nil;
     FBotaoNovo := nil;
end; { of AutoInitialize }

{ Resets prop of component type if referenced component deleted }
procedure TPessoa.Notification(AComponent : TComponent; Operation : TOperation);
begin
     inherited Notification(AComponent, Operation);
     if Operation <> opRemove then
        Exit;
     { Has a component referenced by a property of 
       this component been deleted?  If so, update 
       the property. }
     if AComponent = FPrimeiroNome then
        FPrimeiroNome := nil;
     if AComponent = FSobrenome then
        FSobrenome := nil;
     if AComponent = FDataNascimento then
        FDataNascimento := nil;
     if AComponent = FBotaoNovo then
        FBotaoNovo := nil;
end;

{ Method to free any objects created by AutoInitialize }
procedure TPessoa.AutoDestroy;
begin
     { No objects from AutoInitialize to free }
end; { of AutoDestroy }

{ Write method for property BotaoNovo }
procedure TPessoa.SetBotaoNovo(Value : TButton);
begin
     { Use Assign method because TButton is an object type }
     FBotaoNovo:=Value;
     Value.OnClick:=CadastraNovaPessoa;
end;

{ Method to generate OnNovaPessoa event }
procedure TPessoa.NovaPessoa;
begin
     { Has the application assigned a method to the event, whether
       via the Object Inspector or a run-time assignment?  If so,
       execute that method }
     if Assigned(FOnNovaPessoa) then
        FOnNovaPessoa(Self)
end;

constructor TPessoa.Create(AOwner: TComponent);
begin
     { Call the Create method of the parent class }
     inherited Create(AOwner);

     { AutoInitialize sets the initial values of variables and      }
     { properties; also, it creates objects for properties of       }
     { standard Delphi object types (e.g., TFont, TTimer,           }
     { TPicture) and for any variables marked as objects.           }
     { AutoInitialize method is generated by Component Create.      }
     AutoInitialize;

     { Code to perform other tasks when the component is created }

end;

destructor TPessoa.Destroy;
begin
     { AutoDestroy, which is generated by Component Create, frees any   }
     { objects created by AutoInitialize.                               }
     AutoDestroy;

     { Here, free any other dynamic objects that the component methods  }
     { created but have not yet freed.  Also perform any other clean-up }
     { operations needed before the component is destroyed.             }

     { Last, free the component by calling the Destroy method of the    }
     { parent class.                                                    }
     inherited Destroy;
end;

procedure TPessoa.Loaded;
begin
     inherited Loaded;

     { Perform any component setup that depends on the property
       values having been set }

end;

function TPessoa.Execute : Boolean;
begin
     { Perform the component operation }

     { Return True if the operation was successful, False otherwise }
     Result := True
end;

procedure TPessoa.CadastraNovaPessoa (Sender: TObject);
     { Internal declarations for method }
     { type }
     { . . . }
      var
      FCadastro : TCadPessoa;

begin
     Application.CreateForm (TCadPessoa,FCadastro);
     FCadastro.Pessoa.FPrimeiroNome.Text:=FPrimeiroNome.Text;
     FCadastro.ShowModal;

end;


end.
